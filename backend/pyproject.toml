[project]
name = "prestagri-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "prestagri-catala",
]

[tool.uv.workspace]
members = [
    "import/prestagri-catala",
]

[tool.uv.sources]
prestagri-catala = { workspace = true, editable = true }
